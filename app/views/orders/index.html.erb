<div class="container">
  <div class="starter-template">
    <h1>Order LIST</h1>

    <table class="table table-hover">
      <thead>
        <tr>
          <th>#</th>
          <th>FULL NAME</th>
          <th>EMAIL</th>
          <th>LAPTOP SERIAL NUMBER</th>
          <th></th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% @orders.each do |order| %>
        <tr>
          <td></td>
          <td><%= order.name %></td>
          <td><%= order.email %></td>
          <td><%= order.laptop_number %></td>
          <td><%= link_to "SHOW", order_path(order) %></td>
          <td><%= link_to "EDIT", edit_order_path(order) %></td>
          <td><%= link_to "DELETE", order, method: :delete, data: {confirm: "Are you sure? You can’t cancel this action."}%></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
