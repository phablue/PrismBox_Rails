<!DOCTYPE html>
<html>
  <head>
    <title>Prismbox</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="container"><br>
      <div class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="/">Prism Box</a>
          </div>
          <%= form_tag login_url, class: "navbar-form navbar-right" do %>
            <div class="form-group">
              <%= text_field_tag :email, params[:email], placeholder: "EMAIL", class: "form-control" %>
            </div>
            <div class="form-group">
              <%= password_field_tag :password, params[:password], placeholder: "password", class: "form-control" %>
            </div>
            <%= submit_tag "Sign in", class: "btn btn-primary" %>
          <% end %>
          <h5 class="make_account navbar-right">New to PrismBox? <%= link_to "Here", new_user_path %> </h5>
        </div>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
